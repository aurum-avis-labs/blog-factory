# Copy this file to .github/workflows/deploy-blog-update.yml in each landing page repo.
# Set the BLOG_BRAND repository variable to the brand ID (e.g., "aurum").
# Requires AZURE_STATIC_WEB_APPS_API_TOKEN secret for deployment.

name: Deploy Blog Update

on:
  repository_dispatch:
    types: [blog-update]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci

      - name: Fetch blog content and build
        run: npm run build
        env:
          BLOG_BRAND: ${{ vars.BLOG_BRAND }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: ./dist
          skip_app_build: true
